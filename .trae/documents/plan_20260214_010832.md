## VisionAPI 服务修复计划

### 问题诊断结果
- **根本原因**：视觉服务 `qwen3_vl_service.py`（端口 5003）没有启动
- **模型文件**：已存在于 `models/vision/qwen3-vl-4b` 目录
- **启动脚本**：`launcher.bat` 缺少视觉服务启动逻辑

### 修复步骤

1. **修改 launcher.bat**
   - 在步骤 [2/5] 后添加视觉服务启动检查
   - 自动启动 `qwen3_vl_service.py`（端口 5003）
   - 添加服务状态检查

2. **验证修复**
   - 检查端口 5003 是否有服务监听
   - 测试 `/api/vision/status` 端点
   - 验证前端 VisionAPI 功能正常

### 预期结果
- 视觉服务自动启动并监听端口 5003
- 前端可以正常调用图片分析、OCR、描述功能